import React from 'react'

export default function UseState() {
    /*
    hook là những cái hàm thôi,khi dùng hàm để ý cho thầy đầu vào của hàm
    đầu ra trả về kết quả là gì?
    useState dùng để quản lý trạng thái của dữ liệu
    useState tra ve 1 mang co 2 phan tu:
         +phan tu dau tien la gia tri truyen vao useState
         +
    */
    const result = useState(0);
    console.log(result);
    let arr = [5,6];

  return (
    <div>UseState</div>
  )
}
